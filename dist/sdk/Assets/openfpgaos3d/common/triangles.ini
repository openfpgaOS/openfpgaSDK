[os]
ELF=triangles.elf
ARGS=
# Requires the hardware vertex-triangle bitstream.  The chip32 loader scans
# this .ini for the literal "os30" before configuring the FPGA and loads
# os30.rbf_r when it is found (otherwise os25.rbf_r).  Keep this line.
VARIANT=os30
